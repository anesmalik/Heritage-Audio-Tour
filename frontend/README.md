# Heritage Audio Tour — Frontend

Next.js 16 frontend for the Echoes of Mesopotamia audio tour app.

See the [root README](../README.md) for full setup and architecture docs.

## Development

```bash
npm install
npm run dev     # http://localhost:3000
npm run build   # production build
npm run lint
```

Requires the backend API running on `http://localhost:8000`.
